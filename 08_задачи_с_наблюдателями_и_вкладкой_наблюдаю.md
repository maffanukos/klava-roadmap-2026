# Проблема №8: задачи с наблюдателями и вкладкой «Наблюдаю»

## Бизнес-цель

Сделать в Клаве единый рабочий контур по задачам, где видно не только личные задачи, но и задачи, за которыми сотрудник наблюдает, чтобы снизить потери контекста и ускорить контроль исполнения.

---

## Продуктовая цель

Реализовать модуль задач с ролями:

- автор задачи;
- исполнитель;
- наблюдатель.

Ключевое отличие от референса: поддержка **наблюдателей** и отдельной вкладки `Наблюдаю` со сводной таблицей.

---

## Scope

### S1. Создание и назначение задач

- Создание задачи: заголовок, описание, срок.
- Назначение **одного** исполнителя.
- Добавление наблюдателя(ей).

### S2. Раздел «Задачи»

- Вкладка `Мои` (где я исполнитель/автор).
- Вкладка `Наблюдаю` (где я в роли наблюдателя).

### S3. Вкладка «Наблюдаю» (сводная таблица)

Колонки:

- Кому назначено;
- Заголовок;
- Описание;
- Срок выполнения;
- Статус (`выполнено` / `не выполнено`).

### S4. Группировка и фильтрация

- Группировка по любым полям (исполнитель, срок, статус, автор и т.д.).
- Фильтрация по любым полям.
- Комбинируемые фильтры (несколько условий сразу).

### S5. Доступ к задаче и связь с чатом

- Ссылка задачи доступна только:
  - автору,
  - исполнителю,
  - наблюдателям задачи.
- Задача, созданная из composer, автоматически связывается с текущим чатом/комнатой.

---

## Зафиксированные продуктовые решения

1. Ответственный в задаче — строго один.
2. На этапе MVP комментарий — один, “последний комментарий”.
3. Переход по номеру задачи разрешен только автору/исполнителю/наблюдателям.
4. Статус задачи могут менять: исполнитель, автор и наблюдатель.
5. При создании задачи из composer она привязывается к текущему чату/диалогу/ВКС-контексту.
6. При попытке открыть задачу без доступа: редирект в список задач без раскрытия деталей + микросообщение об ошибке "Нет доступа к задаче номер T-xxxx".
7. Если исполнитель удален/деактивирован, задача автоматически переназначается на автора.

---

## Что реализовано сейчас (UI MVP)

- В левом меню добавлен новый раздел `Задачи` между `ВКС` и `Контакты`.
- Переход по пункту открывает отдельную страницу задач.
- На странице добавлены вкладки `Мои` и `Наблюдаю`.
- Добавлена сводная таблица с полями:
  - кому назначено,
  - заголовок,
  - описание,
  - срок выполнения,
  - статус.
- Добавлены фильтры:
  - поиск,
  - фильтр по статусу.
- Добавлена группировка:
  - без группировки,
  - по исполнителю,
  - по статусу,
  - по сроку.
- Добавлены верхние счетчики `Новые` и `Просрочено` (пока как UI/MVP слой).
- В шапке раздела добавлена кнопка `Создать задачу`.
- Добавлено создание задачи из composer-меню (`...` в поле сообщения) через пункт/кнопку `Создать задачу`.
- Реализован pop-up `Создать задачу` с полями:
  - заголовок,
  - описание,
  - срок,
  - ответственный(е),
  - наблюдатели.
- Для `Ответственный(е)` и `Наблюдатели` реализованы красивые чипы (аватар + имя), поиск и выпадающие подсказки.
- Автоподстановка по умолчанию:
  - в личном чате (DM) `Ответственный(е)` = собеседник;
  - `Наблюдатели` = создатель задачи (с возможностью удалить и добавить других).
- У каждой задачи реализован номер-ссылка (`T-xxxx`).
- По клику на строку или номер задачи открывается pop-up задачи.
- В pop-up задачи для своих задач доступно:
  - комментарий (необязательно),
  - смена статуса `выполнено / не выполнено`.
- Добавлен deep-link формат `#/tasks?task=T-xxxx` для открытия задачи по ссылке.

### Текущие технические ограничения (до backend-этапа)

- Сейчас данные задач локальные (`mock` / `localStorage`), не серверные.
- Deep-link пока клиентский (`#/tasks?task=...`), без backend-резолва задачи.
- Права ролей и уведомления пока не заведены на реальный backend.

---

## Декомпозиция по задачам

## Epic A. Модель данных задач

### A1. Сущности и роли

**Подзадачи:**

- A1.1 Ввести сущность `Task`.
- A1.2 Ввести роли: `author`, `assignee`, `watcher`.
- A1.3 Поддержать множественных наблюдателей.

### A2. Статусы и сроки

**Подзадачи:**

- A2.1 Базовый статус: `done` / `not_done`.
- A2.2 Дата/время дедлайна.
- A2.3 История изменения статуса (минимальный audit trail).

**DoD:**

- роль наблюдателя технически полноценна и отделена от исполнителя.

---

## Epic B. UI создания/редактирования задачи

### B1. Форма задачи

**Подзадачи:**

- B1.1 Заголовок.
- B1.2 Описание.
- B1.3 Срок выполнения.
- B1.4 Выбор одного исполнителя.
- B1.5 Выбор наблюдателей.
- B1.6 Автоподстановка исполнителя для DM.
- B1.7 Автоподстановка наблюдателя (создатель задачи).

### B2. Редактирование

**Подзадачи:**

- B2.1 Изменение всех полей.
- B2.2 Изменение состава наблюдателей.
- B2.3 Обновление статуса.
- B2.4 Добавление/изменение последнего комментария в pop-up задачи.

**DoD:**

- задачу можно создать, назначить и редактировать без обходных сценариев.

---

## Epic C. Раздел «Задачи»: вкладки и таблица

### C1. Вкладка `Мои`

**Подзадачи:**

- C1.1 Список задач, где пользователь исполнитель/автор.
- C1.2 Базовые фильтры.
- C1.3 Кнопка `Создать задачу` в header.

### C2. Вкладка `Наблюдаю`

**Подзадачи:**

- C2.1 Отдельный data-source по роли watcher.
- C2.2 Таблица с колонками из S3.
- C2.3 Пагинация/виртуализация при больших объемах.
- C2.4 Клик по задаче открывает pop-up карточку.
- C2.5 Номер задачи как ссылка.
- C2.6 Проверка доступа к задаче по роли (author/assignee/watcher).

**DoD:**

- наблюдатель видит все нужные задачи в отдельной вкладке.

---

## Epic D. Группировка и фильтрация «как хочешь»

### D1. Универсальная фильтрация

**Подзадачи:**

- D1.1 Фильтры по всем полям таблицы.
- D1.2 Комбинирование условий (AND/OR — MVP: AND).
- D1.3 Сброс/сохранение набора фильтров.

### D2. Универсальная группировка

**Подзадачи:**

- D2.1 Группировка по выбранному полю.
- D2.2 Перегруппировка на лету.
- D2.3 Сортировка внутри групп.

**DoD:**

- пользователь может быстро менять разрезы данных без разработки новых экранов.

---

## Epic E. Права, уведомления, прозрачность

### E1. Права ролей

**Подзадачи:**

- E1.1 Автор, исполнитель и наблюдатель могут менять статус выполнения.
- E1.2 Автор управляет составом участников.
- E1.3 Доступ к задаче по ссылке ограничен ролями задачи.

### E2. Уведомления

**Подзадачи:**

- E2.1 Уведомление о назначении исполнителю.
- E2.2 Уведомление наблюдателю о добавлении.
- E2.3 Уведомление о смене статуса/срока.

---

## Инструкции разработчикам (человеческие)

### 1) Архитектура

- Сразу проектировать под множественные роли, не “приклеивать” наблюдателей поверх assignee.
- Отделить слой query/filter/group от конкретного UI, чтобы не дублировать логику.
- Жестко зафиксировать single-assignee в модели и валидации API/UI.

### 2) UI

- Таблица `Наблюдаю` должна быть читаемой при длинных описаниях (ellipsis + раскрытие).
- Группировка/фильтры не должны блокировать интерфейс на больших списках.
- Pop-up создания задачи не должен раздуваться по высоте экрана.
- Списки пользователей в pickers должны открываться только при вводе поиска.

### 3) Backend

- Дать единый endpoint списка задач с role-based фильтрами (`my`, `watching`, `all-permitted`).
- Отдавать серверную пагинацию и сортировку.
- Добавить связь задачи с источником создания (roomId / contextType).

### 4) Качество

- Обязательно покрыть сценарии смены ролей (наблюдатель -> исполнитель и обратно).
- Проверить согласованность прав в UI и API.

---

## Инструкции для ИИ (по Epic)

### AI-Epic A (модель)

**Промпт:**

> Реализуй модель задач с ролями author/assignee/watcher и базовым статусом done/not_done.  
> Поддержи множественных наблюдателей и дедлайн.  
> Исполнитель строго один.  
> Не начинай с UI, сначала стабилизируй data model и API контракт.

### AI-Epic B (форма задачи)

**Промпт:**

> Реализуй UI формы создания/редактирования задачи: title, description, due date, assignees, watchers, status, comment.  
> Сохрани валидации и минимизируй визуальный шум.

### AI-Epic C (вкладки задач)

**Промпт:**

> Добавь в раздел задач вкладки `Мои` и `Наблюдаю`, кнопку `Создать задачу` в header и pop-up карточки по клику.  
> Во вкладке `Наблюдаю` выведи таблицу: assignee, title, description, due date, done/not_done + task id link.

### AI-Epic D (группировка/фильтры)

**Промпт:**

> Реализуй универсальную группировку и фильтрацию таблицы задач по любым доступным полям.  
> MVP: AND-комбинация фильтров, быстрый сброс, сортировка.

### AI-Epic E (права/уведомления)

**Промпт:**

> Реализуй права для ролей задач и уведомления по событиям назначения/изменения статуса/дедлайна.  
> Статус могут менять author/assignee/watcher.  
> Доступ к карточке задачи по ссылке ограничь только ролями задачи.

---

## Тест-кейсы для QA

## F1. Создание и назначение

### TC-F1-01 Создание задачи с наблюдателями

- Шаги: создать задачу, назначить исполнителя и 2 наблюдателей.
- ОР: задача создана, исполнитель строго один, наблюдатели сохранены корректно.

### TC-F1-02 Редактирование состава наблюдателей

- Шаги: добавить/удалить наблюдателя.
- ОР: изменения применены без потери остальных полей.

### TC-F1-03 Автоподстановка полей

- Шаги: открыть создание задачи из DM и из группового чата.
- ОР: в DM исполнитель подставлен собеседник; наблюдатель подставлен создатель в обоих случаях.

## F2. Вкладка «Наблюдаю»

### TC-F2-01 Отображение таблицы

- Шаги: зайти во вкладку `Наблюдаю`.
- ОР: колонки соответствуют ТЗ.

### TC-F2-02 Корректный скоуп

- Шаги: проверить задачи, где пользователь не watcher.
- ОР: такие задачи не отображаются.

### TC-F2-03 Открытие по номеру

- Шаги: нажать на `T-xxxx` в таблице.
- ОР: открывается pop-up задачи.

## F3. Статусы

### TC-F3-01 Переключение выполнено/не выполнено

- Шаги: исполнитель меняет статус.
- ОР: статус сразу отражается у наблюдателей.

### TC-F3-02 Комментарий в pop-up

- Шаги: открыть свою задачу, добавить комментарий.
- ОР: “последний комментарий” сохраняется и обновляется.

### TC-F3-03 Изменение статуса по ролям

- Шаги: проверить смену статуса автором, исполнителем и наблюдателем.
- ОР: у всех трех ролей изменение статуса доступно.

## F4. Фильтры и группировка

### TC-F4-01 Фильтр по исполнителю

- Шаги: включить фильтр по конкретному assignee.
- ОР: остаются только его задачи.

### TC-F4-02 Группировка по статусу

- Шаги: включить grouping по status.
- ОР: задачи корректно разложены по группам.

### TC-F4-03 Комбинированные фильтры

- Шаги: status + due date + assignee.
- ОР: выдача корректна.

### TC-F4-04 Создание из composer

- Шаги: открыть меню `...` в composer, выбрать `Создать задачу`.
- ОР: открывается pop-up создания задачи, после сохранения задача появляется в таблице и связана с текущим чатом.

### TC-F5-03 Доступ по ссылке задачи

- Шаги: открыть `T-xxxx` под пользователем вне состава задачи.
- ОР: выполняется редирект в список задач, детали не раскрываются, показано микросообщение "Нет доступа к задаче номер T-xxxx".

### TC-F5-04 Удаление/деактивация исполнителя

- Шаги: деактивировать или удалить текущего исполнителя задачи.
- ОР: задача автоматически переназначается на автора; задача остается доступной в списках и не теряет дедлайн/статус.

## F5. Права

### TC-F5-01 Watcher ограничения

- Шаги: watcher пытается менять запрещенные поля.
- ОР: изменение недоступно.

### TC-F5-02 Автор и исполнитель

- Шаги: проверить, что у автора и исполнителя соответствующие права.
- ОР: права соответствуют матрице ролей.

---

## Оценка часов

### MVP (ядро + вкладка Наблюдаю + базовые фильтры/группы)

- Frontend: 34-50 ч
- Backend: 26-40 ч
- QA: 18-28 ч
- Design: 8-12 ч
- **Итого: 86-130 ч**

### Full (расширенные фильтры/группы, уведомления, hardening)

- Frontend: 58-84 ч
- Backend: 44-66 ч
- QA: 30-46 ч
- Design: 12-18 ч
- **Итого: 144-214 ч**

